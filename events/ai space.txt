add_namespace = aispace
#来自中国的支援
news_event = {
	id = aispace.1
	hidden = yes
	fire_only_once = yes
	mean_time_to_happen = { days = 5 }
	trigger = {
			tag = SOV
			is_ai = yes
			date > 1957.10.2
			date < 1957.11.2
			NOT = { has_country_flag = Satellite_succes }
	}
	immediate = {
		set_country_flag = Satellite_succes
		news_event = { id = space.51 }
		add_to_variable = { influences = 90 } 
	}
	
	option = {
		name = news.51.a
	}
}
news_event = {
	id = aispace.2
	hidden = yes
	fire_only_once = yes
	mean_time_to_happen = { days = 5 }
	trigger = {
			tag = USA
			is_ai = yes
			date > 1957.10.2
			date < 1957.11.2
			NOT = { has_country_flag = Satellite_succes }
	}
	immediate = {
		set_country_flag = Satellite_succes
		news_event = { id = space.57 }
		add_to_variable = { influences = 20 } 
	}
	
	option = {
		name = news.51.a
	}
}
###加加林
news_event = {
	id = aispace.10
	hidden = yes
	fire_only_once = yes
	
	trigger = {
			tag = SOV
			is_ai = yes
			date > 1961.4.2
			date < 1961.6.2
			NOT = { has_country_flag = manned_space_succes }
	}
	immediate = {
		set_country_flag = manned_space_succes
					news_event = { id = space.60 }
					add_to_variable = { influences = 300 } 
					USA = { add_to_variable = { influences = -100 } }
					play_song = SOV_19
	}
	
	option = {
		name = news.51.a
	}
}
###莱卡
news_event = {
	id = aispace.11
	hidden = yes
	fire_only_once = yes
	mean_time_to_happen = { days = 5 }
	trigger = {
			tag = SOV
			is_ai = yes
			date > 1957.12.2
			date < 1958.1.2
			NOT = { has_country_flag = space_dog_succes }
	}
	immediate = {
		set_country_flag = space_dog_succes
		news_event = { id = space.9 }
		add_to_variable = { influences = 90 } 
	}
	
	option = {
		name = news.51.a
	}
}